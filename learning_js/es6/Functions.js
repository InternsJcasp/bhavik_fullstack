// Arrow Functions

// Normal Function:
function addTwoNumbers(a, b) {
  return a + b;
}

// Arrow Function:
const subtractTwoNumbers = (a, b) => {
  return a - b;
};

// Implicit Return:
const multiplyTwoNumbers = (a, b) => a * b;
