// Window Object is use to represent the browser/window Tab in which your Website is running. It's basically the top-level browser Object.

// console.log(window);

// Window Properties: window.innerWidth, window.innerHeight, window.location, window.document, window.navigator

// Alert: window.alert("Welcome")

// Timer: window.setTimeout(() => { console.log("Hello");}, 2000)

// Open a new Window: window.open("https://")

// Scroll: window.scrollTO(0,0);

// Browser Window Size:
// console.log(window.innerWidth);
// console.log(window.innerHeight);

// Reload Page: window.location.reload();

// current URL: console.log(window.location.href);

// Document through Window: window.document

// Window Events: window.addEventListener("resize", () => {console.log("Window Resized")})

// When will we use window ?
// Whenever we need something related to the browser window/page environment.

// Examples: Screen Dimensions, Timers, Scrolling, Alerts, URL, Browser Events, Opening Windows
