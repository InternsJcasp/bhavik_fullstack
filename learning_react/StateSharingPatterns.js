// In a real app, We usually have multiple contexts, not one giant context.

// Typical Split:
// AuthContext: user, login, logout, loading, error.
// ThemeContext: theme, toggleTheme, setTheme.
// NotificationContext: notifications list, count, add/remove Notification
